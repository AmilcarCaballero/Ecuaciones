#include <stdio.h>
#include <stdlib.h>
#include <conio.h>

using namespace std;

#define F(x)pow(x,2)
#define a 3.0F
#define h 0.01

int main(){
	float df = (f(a+h) -f(a))/h;
	cout << df << end1;
  return 0;
}
